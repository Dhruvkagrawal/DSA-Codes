function stockItUp(N, elements) {
  sum = 0;
  if (elements[0] === 1) {
    sum = sum = sum + 110 * 5;
  }
  if (elements[1] === 1) {
    sum = sum = sum + 120 * 2;
  }
  if (elements[2] === 1) {
    sum = sum = sum + 42 * 2;
  }
  if (elements[3] === 1) {
    sum = sum = sum + 53 * 3;
  }
  if (elements[4] === 1) {
    sum = sum = sum + 40 * 2;
  }
  if (elements[5] === 1) {
    sum = sum = sum + 32 * 5;
  }
  if (elements[6] === 1) {
    sum = sum = sum + 126 * 2;
  }
  console.log(sum * N);
}
